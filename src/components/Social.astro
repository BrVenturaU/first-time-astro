---
const { platform, username } = Astro.props;
---

<a
  class="py-2 px-4 bg-purple-600 no-underline hover:bg-purple-900 transition-colors ease-in duration-150"
  href={`https://www.${platform}.com/${username}`}
  target="_blank">{platform}</a
>